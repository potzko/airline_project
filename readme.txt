I decided to split my code to 2 parts, the front end, and the backend
the backend is a rest api allowing for crud over my sql as well as a bit of proccessing for the user login/register (passwords are salted peppered sha256)

the front end is react that runs calls to the backend
